import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ryltsov.alex.plugins.device.example',
  appName: 'device-example-app',
  webDir: 'www/browser'
};

export default config;
